function Navbar () {

    return (

        <nav>

            <button>Home</button>

            <button>Collection</button>

            <button>Search</button>

        </nav>
    );
}

export default Navbar